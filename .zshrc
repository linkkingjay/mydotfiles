PATH=$PATH:$HOME/.rvm/bin # Add RVM to PATH for scripting

alias ls='ls -F --color=auto'
alias ll='ls -l'
alias la='ls -a'
alias lla='ls -al'

alias www="cd ~/www"
alias h="cd ~/homework"

alias goagent="python ~/google_appengine/goAgent/local/proxy.py";
alias vps="ssh linkkingjay@49.212.150.224";
alias azure="ssh link@link.chinacloudapp.cn";
alias dos="dosemu -dumb"
alias art="sudo /etc/init.d/apache2 restart"
alias du="du -csh"

# tmux stuffs
alias tl='tmux list-sessions'
alias ta='tmux attach-session -t'

/usr/games/fortune
echo '=========================='
